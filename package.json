{
    "name": "rdvpriority-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "cd backend && npm start",
        "build": "cd frontend && npm install && npm run build"
    },
    "dependencies": {
        "playwright": "^1.58.2",
        "socket.io": "^4.8.3"
    }
}
